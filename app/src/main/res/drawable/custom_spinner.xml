<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <!-- This is the actual spinner background -->
        <selector >
            <item>
                <shape>
                    <gradient android:angle="90" android:endColor="#ffffff" android:startColor="#ffffff" android:type="linear" />

                    <stroke android:width="1dp" android:color="@color/primary" />

                    <corners android:radius="20dp" />

                    <padding android:bottom="3dp" android:left="3dp" android:right="3dp"  />
                </shape>
            </item>
        </selector>
    </item>

    <!-- This is the spinner drawable, use your custom drawable aswell. android:right does the magin to separate this drawable from  the spinner content-->
    <item android:drawable="@drawable/ic_arrow_down_primary"
        android:gravity="center_vertical|right" android:right="10dp"/>
</layer-list>
